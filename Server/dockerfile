FROM openjdk

EXPOSE 8080
EXPOSE 8000

ARG ADDITIONAL_OPTS

ENV ADDITIONAL_OPTS=${ADDITIONAL_OPTS}

WORKDIR /app

COPY target/loja-0.0.1-SNAPSHOT.jar app/loja.jar

CMD java ${ADDITIONAL_OPTS} -jar app/loja.jar